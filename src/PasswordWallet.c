#include "PasswordWallet.h"

void next_password()
{
    /*
    uint8_t pwd[16];
    memory_read((uint8_t*)(password_id * 16), pwd, 16);
    uint8_t i = 0;
    for(i = 0; i < 16; ++i)
    	pwd1[i] = pwd[i];
    ++password_id;
    decrypt_password(FIRST_PWD_OFFSET + 6 + password_id * 136, FIRST_PWD_OFFSET + 56 + password_id * 136);
    //*/
}

void decrypt_password(uint16_t* start, uint16_t* end)
{

}
